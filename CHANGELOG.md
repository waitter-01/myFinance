# 版本变更记录

本项目采用[语义化版本](https://semver.org/lang/zh-CN/)管理版本，版本号格式为 `主版本.次版本.修订版本`。

## [0.12.1] - 2026-08-26

### 银行截图 OCR 修复

- 修复中信银行长截图第一条流水可能未进入导入预览的问题，按完整日期行恢复对应金额区域。
- 修复工商银行截图裁剪后仍对整图缩放、导致局部金额和时间识别失效的问题。
- 为中信银行秒级时间增加离线数字专用 OCR，重点纠正数字 `3` 与 `5` 的系统性混淆。
- 中信银行固定区域会进行多尺度、灰度和阈值识别；可信数字纠错自动采用，真正存在分歧的记录仍保留人工核对入口。
- 增加第三方组件及模型许可说明，所有截图和数字识别仍在本机完成。

### 质量

- 使用实际中信银行和工商银行截图回归首条、末条、金额及秒级时间识别。
- 自动化测试总数提升至 26 项。

## [0.12.0] - 2026-08-25

### 银行截图识别

- 新增中信银行借记卡“交易明细”长截图识别，支持秒级交易时间、带符号金额、交易对方和余额辅助行过滤。
- 新增工商银行“查询明细”截图识别，可组合月份标题、左侧日期和账户行秒级时间，并区分工资收入、普通支出和还款转账。
- 银行页面识别优先于正文中的“支付宝”“微信”等支付渠道文字，避免把银行流水误判为支付平台账单。
- 银行截图同样使用自动分类规则，工资、交通、餐饮、购物等明显交易会在导入预览中获得建议分类。

### 跨来源重复识别

- 银行卡扣款与支付宝/微信流水在金额、时间和交易对方相符时，会进入“疑似重复”页面并说明可能为同一笔支付。
- 银行记录只写“支付宝”“微信支付”或“财付通”时，使用更严格的 5 分钟时间范围进行判断，降低同额消费误判。
- 还款流水不会与原始消费自动关联，避免把信用卡、月付等账单还款错误当成当日平台消费。

### 多设备同步

- S3 同步改为带云端对象版本校验的按记录双向合并；另一设备并发写入时自动重新下载、合并并重试，防止旧快照覆盖新账本。
- 不同设备新增的记录取并集；同一记录冲突时保留更新时间较新的完整版本；删除标记也按更新时间参与合并。
- 新增“自动合并本地修改”开关：本地保存后约 10 秒批量同步，并每 5 分钟拉取其他设备的修改，避免每次按键都请求云端。
- 同步结果显示冲突数量与并发重试次数，断网或同步失败不会影响已经保存在本地的账本。

### 质量

- 新增中信银行、工商银行和银行/支付平台跨来源重复识别测试，自动化测试总数提升至 22 项。

## [0.11.0] - 2026-08-25

### 周期性支出

- 将“订阅与月卡”扩展为“周期性支出”，统一管理数字订阅、游戏权益、生活服务、房租、物业与车位、保险、教育服务等固定支出。
- 流水可记录周期支出类型、覆盖开始日期、覆盖月数、下次付款日期和是否为必要支出。
- 同时展示本月实际付款、月度分摊成本、周期项目数量和未来 90 天预计付款，避免多月房租或年费会员扭曲日常月均成本。
- 周期列表展示最近付款、费用覆盖期间、月均成本、下次付款和支出性质；旧订阅消费及覆盖月数字段保持兼容。
- 新增“住房租金”预设分类，并自动识别房租、租金和公寓租赁等关键词。
- S3 同步与 CSV 导出包含新增周期字段，旧版 S3 数据仍可正常读取。

### 消费洞察

- 收支趋势柱状图增加悬停金额提示，悬停柱体会放大并提亮。
- 新增主要消费占比环形图，将详细分类归并为餐饮饮品、居住生活、交通、购物、休闲娱乐、健康保障、学习、人情、订阅服务和其他。
- 保留详细“分类去向”列表，并支持点击分类弹出当前统计周期的完整流水明细。
- 分类明细窗口显示时间、金额、账户、交易对方和备注，并汇总笔数与金额。

### 质量

- 新增主要分类归并和周期性支出持久化测试，自动化测试总数提升至 17 项。
- 使用隔离示例账本完成消费占比图、分类下钻窗口和周期性支出页面的实机界面检查。

## [0.10.1] - 2026-08-25

### 界面改进

- 流水表头排序按钮改为横向撑满对应列的低干扰圆角控件，统一文字、图标和内边距，不再紧贴文字形成狭小边框。
- 表头与流水行使用相同的列内边距，并移除列表项容器的默认额外留白，使时间、类型、金额、分类、账户、商户和备注纵向对齐。
- 移除“收支类型”“分类”“账户”三个重复筛选按钮，统一保留“高级筛选”入口。
- 高级筛选按钮会汇总显示当前生效的筛选条件数量。

## [0.10.0] - 2026-08-25

### 新增

- 全部流水增加显式“批量管理”模式，进入后才显示多选框，避免日常浏览时产生视觉干扰或误选。
- 增加圆角悬浮批量操作栏，可选择全部筛选结果，并批量修改分类、调整账户或删除流水。
- 批量删除会同步写入删除标记，防止已删除流水在后续 S3 同步时重新出现。
- 支持将当前筛选结果或选中流水导出为 CSV，导出内容包含时间、类型、金额、分类、账户、商户、备注和来源。
- 支持保存、应用和删除常用筛选方案，并同时保存搜索条件、日期范围、分组方式及排序规则。
- 本月、最近 7 天等动态日期条件在常用筛选中保持动态，每次应用时按当天重新计算。

### 改进

- 批量操作栏会显示实时选中数量，分类和账户选项与当前账本配置保持同步。
- 常用筛选入口显示已保存方案数量，无流水结果时仍可保存当前筛选配置。
- 增加批量操作、删除标记和常用筛选持久化自动化测试，测试总数提升至 15 项。

## [0.9.1] - 2026-08-25

### 修复

- 关闭流水列表的黏性分组标题，避免快速滚动时多个日期标题堆叠在同一位置。
- 为分组标题设置独立容器高度、左右两列和完整横向拉伸，日期与汇总不再相互覆盖。
- 日期标题调整为 14 像素半粗体，右侧汇总调整为 12 像素次级文字，统一列表字号层级。

### 界面改进

- “筛选流水”更名为“高级筛选”，标题和说明文字缩小并重新调整间距。
- 高级筛选改为四角圆角悬浮卡片，增加页面外边距、完整描边和柔和阴影，不再贴住窗口边缘。
- 增加流水分组选择：不分组、按天分组、按周分组和按月分组。
- 按周分组统一以周一至周日计算，跨月或跨年时显示完整日期区间；每个分组继续显示净支出、收入和笔数。

## [0.9.0] - 2026-08-25

### 新增

- 全部流水增加 300 毫秒防抖的实时搜索，支持商户、备注、分类、账户、导入来源和收支类型。
- 增加全部时间、本月、上月、最近 7 天、最近 30 天、本年度和自定义日期快捷筛选。
- 增加右侧高级筛选面板，支持收支类型、分类、账户和来源多选，以及金额上下限、未分类、订阅消费、未指定账户筛选。
- 分类和账户筛选列表支持关键词查找，多次搜索时保留已经勾选的项目。
- 所有生效条件以可单独移除的标签展示，并提供筛选数量提示和一键清除。
- 筛选结果实时显示笔数、净支出、收入、退款/报销和筛选结余或净流出。
- 增加时间、金额和商户排序；日期排序时按天分组并显示当日净支出、收入及笔数。
- 增加无结果空状态、整行点击编辑、右键编辑/删除和低干扰的更多操作按钮。

### 查询与性能

- 新增统一的 `TransactionQuery` 查询模型，组合条件全部通过 SQLite 参数化查询执行。
- 排序方式使用固定枚举映射，避免将界面输入直接拼接为 SQL。
- 导入来源自动归并为微信截图、支付宝截图、表格文件等稳定类型，避免每个截图文件名都成为单独筛选项。
- 增加组合条件、汇总口径、特殊条件、来源归并和排序自动化测试，测试总数提升至 14 项。

## [0.8.5] - 2026-08-25

### 界面重构

- 将账单导入预览从受宿主尺寸约束的 `ContentDialog` 重构为独立 WinUI 3 窗口。
- 导入窗口支持拖动、最大化和自由调整大小，并保持与主程序一致的 Mica 背景、圆角卡片、图标和主题资源。
- 窗口变窄时，统计卡片、账户提示、流水修正字段和底部操作区会自动换行重排。
- 设置 780 × 720 的最小可用尺寸，避免用户将窗口缩小到按钮或列表无法操作的状态。

### 修复

- 流水列表现在稳定占用窗口剩余高度，垂直滚动区域不再被对话框宿主裁切。
- “查看最后一条”改为切换到待导入页、选中末条并在布局完成后再次定位，确保滚动实际执行。
- “定位并修改”使用相同的延迟定位机制，问题记录可可靠跳转到对应流水。
- 确认导入和取消按钮固定保留在窗口底部；修正和跳过按钮使用独立操作行，不再因字段过多而显示不全。

## [0.8.4] - 2026-08-25

### 修复

- 根据用户提供的实际微信账单截图完成端到端诊断，确认 8 个金额锚点会生成 8 条解析记录，末条并未丢失。
- 修复导入预览一次只能显示约 7 行且滚动条自动隐藏，导致末条看似缺失的问题。
- 待导入、疑似重复和需要核对列表均改为始终显示垂直滚动条。

### 改进

- 待导入标签页直接显示记录数量，并增加“查看最后一条”按钮，可一键滚动并选中末条。
- 文件统计同时显示文件数和识别记录总数，便于核对“识别数、待导入数、重复数、问题数”。
- 导入预览根据主窗口可用空间自动扩展至更大的宽度和高度，不再始终固定为 1120 × 650。
- 增加账单时间倒序校验；OCR 将数字误读后造成下方流水时间反而更新时，自动标记为需要核对，不擅自修改原始识别结果。
- 使用用户实际截图验证：8 条记录全部保留，并成功发现其中 5 条日期顺序异常。

## [0.8.3] - 2026-08-25

### 修复

- 修复长截图末条已识别金额但商户文字被截图边缘裁切时，识别总数与待导入数量不一致的问题。
- 金额已识别的截图记录不再因商户或时间缺失而直接丢弃，而是保留为带“待核对”标记的候选流水。
- 为缺失商户和缺失时间的问题建立与候选流水的关联，避免问题报告与可编辑记录脱节。

### 改进

- “问题行”更名为“需要核对”，顶部统计卡增加“查看并修正”按钮。
- 可修正问题增加“定位并修改”操作，点击后自动打开待导入流水、选中对应记录并定位编辑框。
- 应用修正或跳过流水后，对应问题自动移除并实时更新数量。
- 存在尚未处理的候选问题时暂时禁用确认导入，防止占位商户或回退日期误写入账本。
- 新增模拟 8 条长截图且末条缺失商户的回归测试，确保 8 条金额记录生成 8 条候选流水。

## [0.8.2] - 2026-08-25

### 界面改进

- 发现疑似重复记录时，导入预览会自动打开“疑似重复（需要确认）”页面。
- 疑似重复统计卡增加“查看并处理”按钮，处理完最后一条后自动回到待导入流水。
- 总览、全部流水和导入预览统一使用财务语义色：支出为克制暖红，收入、退款和报销为绿色，转账为蓝色。
- 金额增加正负号，收支类型增加低饱和圆角标签和左侧色条，颜色不再是判断交易类型的唯一依据。
- 分类改为低饱和圆角标签，不同分类使用稳定配色，改善长流水列表的定位能力。
- 语义色自动适配 Windows 深浅色背景，同时保留 WinUI 中性色主体和列表分隔层级。

## [0.8.1] - 2026-08-25

### 修复

- 修复长截图、Excel 和历史账本使用不同指纹生成规则，导致相同交易无法识别的问题。
- 重复判断增加分钟级交易时间、金额和收支类型组合校验，不再只依赖指纹。
- 同一批次截图或表格内的重复记录也会在写入前识别。
- 用户在预览中修正时间、金额或类型后，会重新检查是否与账本已有记录重复。

### 改进

- 导入预览增加“疑似重复”页面，展示待导入记录、账本已有记录及判定原因。
- 疑似重复记录默认跳过；如果确实是时间和金额相同的两笔交易，可以选择“仍然导入所选”。
- 用户确认保留的同额同时间交易使用独立标识，允许两笔真实交易同时保存在账本中。
- 增加历史指纹兼容、不同收支类型隔离及用户确认保留的自动化测试。

## [0.8.0] - 2026-08-25

### 新增

- 消费洞察升级为统一的周度、月度和年度财务分析，可切换周期并浏览历史周期。
- 增加收入、净支出、结余、储蓄率、日均支出、最大单笔支出和建议支出上限等核心指标。
- 增加原生 WinUI 收支柱状趋势图、分类占比进度图、分类排行与商户排行。
- 自动生成周期摘要、同比上期变化、小额消费判断、主要资金去向和可执行控制建议。
- 周度、月度和年度分析均可导出 CSV，文件包含指标摘要及对应周期流水。
- 增加核心分析服务自动化测试，覆盖周期边界、趋势分桶及关键统计口径。

### 统计口径

- 退款和报销从支出中冲减，形成净支出；收入仅统计真正的收入记录。
- 转账、信用卡还款等账户间资金移动不计入收入或支出，避免重复计算消费。
- 当前周期与上一周期对比时使用相同已过天数，避免未结束周期与完整周期直接比较。
- 分类和商户排行展示消费金额、笔数与占比；小额和可优化消费遵循用户偏好设置。

### 界面改进

- 重新设计洞察页的信息层级、指标卡片、周期导航、图表图例和建议区域，保持 WinUI 3 视觉一致性。
- 图表使用原生控件绘制，不增加第三方图表运行依赖。

## [0.7.0] - 2026-08-25

### 架构调整

- 仓库调整为 Windows 桌面应用单一主项目，移除暂停维护的 Next.js、Prisma、Docker、Ubuntu 部署和 Web 测试代码。
- 移除旧 WPF 回退界面，将仍在使用的模型、导入、存储、分类和 S3 同步逻辑迁入独立 `DuxiuLedger.Core` 项目。
- WinUI 3 主程序迁入 `src/DuxiuLedger.App`，通过正式项目引用依赖 `src/DuxiuLedger.Core`，不再跨项目链接源码文件。
- 增加根目录 `DuxiuLedger.sln`，Visual Studio 和命令行均可一次加载、构建完整项目。
- Logo、Excel 模板、安装定义、构建脚本和产物分别整理到 `assets`、`templates`、`installer`、`scripts` 和 `artifacts`。

### 兼容性

- 本次调整只改变源码仓库结构，不改变本地账本位置、账本格式、S3 同步对象或用户设置，升级无需迁移数据。
- 发布与安装脚本已适配新目录，并继续支持自包含单文件 EXE 和 Inno Setup 安装程序。

## [0.6.4] - 2026-08-25

### 改进

- 安装程序构建脚本支持自动发现 `D:\APPs\Inno Setup 7` 和 `D:\APPs\Inno Setup 6`，优先使用 Inno Setup 7。
- 支持通过 `-InnoCompiler` 参数或 `INNO_SETUP_ISCC` 环境变量指定其他 `ISCC.exe` 路径。
- Setup.exe 使用独秀账本多尺寸图标，与应用、快捷方式和卸载列表保持一致。

### 验证

- 已使用 Inno Setup 7.1.0 成功生成中文 x64 安装程序，并核对产品版本、安装包图标和 SHA-256。

## [0.6.3] - 2026-08-25

### 修复

- 将现有独秀账本 Logo 转换为包含 16–256 像素的多尺寸 Windows ICO，并嵌入 WinUI 3 与 WPF 可执行文件。
- 显式设置 WinUI 3 `AppWindow` 图标，统一标题栏、Windows 任务栏、Alt+Tab 窗口切换器和快捷方式图标。
- 安装程序和卸载列表继续从主 EXE 读取同一图标，不再显示默认应用图标。

### 工程化

- 增加 `scripts/generate-app-icon.ps1`，可从现有 PNG Logo 重复生成标准多尺寸 ICO。

## [0.6.2] - 2026-08-25

### 修复

- 自动识别并忽略旧版本中误存为完整访问 URL 的 Bucket 配置，重新从访问地址与 API 端点提取真实桶名。
- 修复旧错误配置导致 RainS3 返回 `InvalidBucketName / The specified bucket is not valid` 的问题。
- 补全 S3 同步服务的显式文件流引用，使 WinUI 3 和保留的 WPF 工程均可独立编译。

### 验证

- 使用 RainS3 实际端点完成只读连接验证，Bucket 自动识别为 `zxx`；服务返回 `NoSuchKey`，表明端点、签名凭据和桶访问均已通过，等待应用首次创建同步对象。

## [0.6.1] - 2026-08-25

### 新增

- S3 设置支持直接填写对象存储控制台提供的访问地址、API 端点、Access Key 和 Secret Key。
- 根据访问地址和 API 端点自动识别 Bucket；Region、Bucket 覆盖、对象路径和 Path Style 收入高级设置。

### 修复

- `Ctrl+V` 仅在非文本输入区域触发截图识别，编辑凭据、搜索条件、金额和其他文本时恢复系统粘贴行为。

## [0.6.0] - 2026-08-25

### 新增

- 增加用户自主配置的 S3 对象存储同步，支持 AWS S3 以及提供 S3 兼容接口的 Cloudflare R2、MinIO 等服务。
- 支持自定义 Endpoint、Region、Bucket、对象路径、Access Key、Secret Key、临时 Session Token 和 Path Style。
- 使用带版本号的 JSON 同步对象保存流水、账户、分类、预算、储蓄目标和删除记录，并按最后更新时间双向合并。
- S3 Secret Key 和 Session Token 通过 Windows DPAPI 加密保存在当前 Windows 用户配置中。

### 变更

- 完全移除 MySQL 驱动、直连同步服务和相关用户设置，应用不再要求用户安装、维护或开放数据库服务器。
- 启动时自动清理旧版保存的 MySQL 地址、用户名和加密密码等配置。
- 保持本地优先：无网络时仍可正常记账，恢复连接后再与用户指定的 S3 对象合并。
- 数据备份统一使用 `.duxiu` 账本备份文件，界面不再要求用户理解底层存储实现。

### 升级说明

- v0.6.0 不会自动读取旧 MySQL 服务器中的云端内容；本机已有账本不受影响，配置 S3 后首次同步会把本机内容上传到指定对象。
- 每台新电脑都需要单独填写 S3 凭据，普通设置和同步数据不会包含明文 Secret Key。

## [0.5.2] - 2026-08-25

### 新增

- 增加常见商户关键词自动分类，覆盖餐饮饮品、游戏、购物快递、出行、保险、医疗、教育、数码、通讯、居住、旅行和宠物等主要消费。
- 蜜雪冰城、瑞幸等茶饮咖啡商户自动建议为“零食饮料”。
- 增加“保险保障”预设分类，已有数据库会在启动时自动补充。

### 改进

- 导入预览由 WinUI 默认窄对话框调整为 1100 像素宽版圆角布局，与主界面卡片风格保持一致。
- 流水列表增加列标题，扩大交易对方和来源显示空间，完整内容可通过悬停提示查看。
- 问题记录扩大来源、原因和原始内容区域，原始内容支持自动换行。
- 自动分类只替换“未分类”，不会覆盖账单原有分类或用户已经确认的分类；转账仍保持未分类以避免误判。

## [0.5.1] - 2026-08-25

### 新增

- 支持把一张或多张账单图片直接拖入应用窗口识别。
- 支持从剪贴板粘贴图片或已复制的图片文件识别。
- 增加“粘贴截图”按钮和 `Ctrl+V` 快捷键。
- 截图文件选择和拖拽新增 TIFF 格式支持。

### 改进

- 文件选择、拖拽和剪贴板统一使用同一套本地 OCR、预览校正、指纹去重和确认保存流程。

## [0.5.0] - 2026-08-25

### 新增

- 增加微信、支付宝账单列表长截图批量导入入口，支持 PNG、JPG、JPEG 和 BMP。
- 使用 Windows 本地中文 OCR，账单截图和识别文字均不会上传到第三方服务器。
- 超长截图自动分段识别并对重叠区域去重，无需用户预先裁图。
- 分别解析支付宝和微信列表布局，识别交易时间、商户、金额、支出、转账和退款。
- 截图导入沿用交易指纹去重，可一次选择多张图片。
- 导入预览增加金额、方向、分类、商户和时间修正，以及“跳过此条”操作。

### 注意事项

- OCR 结果受截图清晰度、界面版本和系统中文 OCR 组件影响，保存前必须核对预览。
- 截图边缘未完整显示日期的交易会标记为“交易时间需要手动核对”。
- 商户名称在原页面已被省略号截断时，应用无法恢复被隐藏的完整文字。

## [0.4.1] - 2026-08-25

### 修复

- 增加 MySQL 5.x 旧版兼容模式，禁用不兼容的 TLS 协商并改用旧版服务器支持的 `utf8` 字符集。
- 连接测试成功后显示服务器实际版本，便于继续诊断权限和版本问题。
- 兼容模式增加明文传输风险提示，默认仍保持安全优先的 TLS 配置。

## [0.4.0] - 2026-08-25

第三阶段预算、目标、提醒和多端同步版本。

### 新增

- 月度总预算、按月分类预算、预算进度和超支提示。
- 储蓄目标、目标日期、当前已存金额和每月建议储蓄额。
- MySQL 公网直连、本地优先双向合并、删除同步和启动自动同步。
- MySQL 连接测试、TLS 模式选择和同步结果展示。
- 数据库密码通过 Windows DPAPI 加密保存，不进入源码、文档、日志或 Git 历史。
- Windows 每日记账提醒和每周近 7 天支出总结。
- 近 6 个月收入、支出和结余趋势。
- 本月流水 CSV 报告导出。
- “系统建议可减少支出”的分类范围可在偏好设置中修改。

### 改进

- 预算和储蓄目标纳入本地 SQLite 持久化，并参与 MySQL 多端同步。
- 所有同步实体增加稳定唯一标识、更新时间和删除墓碑，按最后更新时间合并。
- MySQL 不可用时不影响本地记账，连接错误不会暴露数据库密码。

### 注意事项

- 公网 MySQL 建议开启 TLS、限制允许连接的 IP，并为应用账户只授予目标数据库权限。
- 首次同步时，数据库账户需要创建 `duxiu_sync_items` 表的权限。
- Windows 提醒在保存偏好设置后通过当前用户的任务计划运行；移动 EXE 后应重新保存一次提醒设置。

## [0.3.0] - 2026-08-25

第二阶段消费分析起步版本。

### 新增

- 支付宝小荷包预设账户。
- 27 个覆盖餐饮、生活、娱乐、游戏、订阅等用途的详细收支分类。
- 分类新增、编辑、排序、停用和安全删除功能，已使用分类支持保留流水并停用。
- “订阅消费”预设分类和 1–120 个月计费周期记录。
- Excel 导入模板及账单解析支持“订阅月数”“计费月数”“覆盖月数”字段。
- 消费洞察页面：小额消费、系统建议可减少支出、最大支出分类和下月建议额度。
- 本月分类排行、商户排行、上月同期可选消费对比和系统建议。

### 改进

- 订阅月均负担按每个项目最近价格除以覆盖月数计算，年度会员和季度会员不再被当作单月成本。
- 手动录入分类改为根据交易类型筛选的可编辑选择框。
- 旧数据库启动时自动增加分类和订阅计费字段，无需重新导入历史流水。

### 已知限制

- 可减少支出目前由系统预设分类估算，后续会开放分析规则设置。
- 预算计划、Windows 系统提醒和 MySQL 多端同步仍在后续阶段。
- 银行账单字段映射向导尚未实现。

## [0.2.0] - 2026-08-25

第一阶段数据准确性更新。

### 新增

- 流水编辑和带二次确认的删除功能。
- 支出、收入、转账、退款和报销五种交易类型。
- 本月净支出统计，自动扣除退款和报销，转账不计入消费。
- 现金、银行卡、信用卡、电子钱包、储蓄、投资和贷款账户管理。
- 账户期初余额、动态余额、停用保护以及流水账户关联。
- 转账的转出账户和转入账户记录。
- 账单导入预览、重复记录统计、问题行原因和导入账户选择。
- 流水按商户、备注、分类和账户搜索。

### 改进

- 微信、支付宝和表格导入时保留退款、报销和转账类型。
- 旧版数据库自动增加账户字段，不需要重新导入历史账单。

### 已知限制

- 已导入但未指定账户的历史流水需要手动编辑关联。
- 预算和分类管理页面仍在建设。
- 银行账单字段映射向导尚未实现。
- 提醒时间可以保存，但 Windows 系统通知尚未接入。

## [0.1.0] - 2026-08-25

首个可使用的桌面 MVP 版本。

### 新增

- 原生 Windows App SDK / WinUI 3 桌面界面。
- Mica 背景、NavigationView 导航和系统深浅色主题控件。
- 中文界面使用 Microsoft YaHei UI，数字和英文优先使用 Segoe UI Variable。
- 本地 SQLite 数据持久化，重启后无需重新导入账单。
- Excel、CSV、微信和支付宝常见账单格式导入。
- 账单指纹去重和手动流水录入。
- 本月收入、支出、结余及最近流水总览。
- 流水搜索、数据备份和数据目录访问。
- 可持久化的小额消费阈值、月度预算和提醒偏好。
- 订阅、会员与游戏月卡识别及近 12 个月月均分摊统计。

### 已知限制

- 流水编辑和删除尚未实现。
- 预算与分类管理页面仍在建设。
- 提醒时间可以保存，但 Windows 系统通知尚未接入。
- 云同步、账户管理、资产负债和多端客户端尚未实现。

[0.8.4]: https://github.com/waitter-01/myFinance/releases/tag/v0.8.4
[0.8.3]: https://github.com/waitter-01/myFinance/releases/tag/v0.8.3
[0.8.2]: https://github.com/waitter-01/myFinance/releases/tag/v0.8.2
[0.8.1]: https://github.com/waitter-01/myFinance/releases/tag/v0.8.1
[0.8.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.8.0
[0.7.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.7.0
[0.6.4]: https://github.com/waitter-01/myFinance/releases/tag/v0.6.4
[0.6.3]: https://github.com/waitter-01/myFinance/releases/tag/v0.6.3
[0.6.2]: https://github.com/waitter-01/myFinance/releases/tag/v0.6.2
[0.6.1]: https://github.com/waitter-01/myFinance/releases/tag/v0.6.1
[0.6.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.6.0
[0.2.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.2.0
[0.3.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.3.0
[0.4.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.4.0
[0.4.1]: https://github.com/waitter-01/myFinance/releases/tag/v0.4.1
[0.5.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.5.0
[0.5.1]: https://github.com/waitter-01/myFinance/releases/tag/v0.5.1
[0.5.2]: https://github.com/waitter-01/myFinance/releases/tag/v0.5.2
[0.1.0]: https://github.com/waitter-01/myFinance/releases/tag/v0.1.0
